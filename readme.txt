The code was run using Eclipse IDE

The input was given in a text file and in the program arguments the path of the text file was provided.

Without a second argument , the program only prints the Maximum Profit and the number of optimal solutions
If the second argument is provided as 1 or more , it will also print the optimal solutions along with Max value and count of optimal solutions